# Development and testing dependencies (not used in Vercel)
-r requirements.txt

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
fpdf2>=2.8.0

# Frontend (local development only)
streamlit>=1.40.0
